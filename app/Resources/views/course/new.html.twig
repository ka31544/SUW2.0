{% extends 'base.html.twig' %}

{% block body %}
<div class="column is-6">
    <h2 class="title">Utworzenie kursu</h2>

    {% if is_granted('ROLE_ADMIN') %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Utwórz" class="primary-button"/>
    {{ form_end(form) }}
    {% endif %}
    <a href="{{ path('course_index') }}"><i class="fa fa-reply fa-2x" title="Wróć do listy kursów"></i></a>
</div>
{% endblock %}
